name: PR Check

permissions:
  contents: read

on:
  pull_request:
    branches:
      - main
jobs:
  Build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: 🏗️ Build, Test
        run: |
          ./gradlew clean build